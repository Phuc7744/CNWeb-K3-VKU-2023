chạy file database-> fashion_shop.sql;
config database tại models/DB.php;

trường hợp lỗi ảnh, lỗi dữ liệu:
    xóa hết img trong public/img/shoes;
    chạy file db.sql(file tạo database với dữ liệu trống);